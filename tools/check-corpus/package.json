{
  "name": "@relay/check-corpus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Validates corpus file counts and byte identity using metadata.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json && node dist/index.js",
    "lint": "eslint src"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "5.9.3",
    "@types/node": "20.19.43"
  },
  "engines": {
    "node": ">=20.20.2 <21"
  }
}
