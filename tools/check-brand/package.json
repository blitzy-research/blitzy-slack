{
  "name": "@relay/check-brand",
  "version": "0.0.0",
  "private": true,
  "description": "Pipeline guard: fails the build on any third-party identity token outside the allowlisted read-only paths.",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist .turbo *.tsbuildinfo",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "start": "pnpm run build && node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "20.19.43",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
