{
  "name": "@relay/measure-frames",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Performs deterministic geometric measurement of specification frames for design-token derivation.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p . --noEmit",
    "measure": "pnpm run build && node dist/src/index.js"
  },
  "dependencies": {
    "sharp": "0.35.3"
  }
}
